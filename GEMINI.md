# Project Context

## Current Status
- Initial setup phase.
- Project structure created.
- Docker environment configured.

## Immediate To-Dos
- [ ] Install dependencies locally (`pip install -r requirements-dev.txt`).
- [ ] Initialize git repo (`git init`).
- [ ] Install pre-commit hooks (`pre-commit install`).
- [ ] Verify Docker build (`docker-compose build`).

## Notes
- The `docs/` folder is intended to be opened as an Obsidian vault or part of one.
- `docs/KANBAN.md` is formatted for the Obsidian Kanban plugin.
