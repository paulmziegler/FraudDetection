services:
  app:
    build: .
    volumes:
      - .:/app
    command: python manage.py run
    deploy:
      resources:
        reservations:
          devices:
            - driver: nvidia
              count: 1
              capabilities: [gpu]
